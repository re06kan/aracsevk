import { Component } from '@angular/core';
import { CommonModule } from '@angular/common';

@Component({
  selector: 'app-suruculer',
  standalone: true,
  imports: [CommonModule],
  template: `
    <div>
      <h2>Sürücüler</h2>
      <!-- içerik -->
    </div>
  `
})
export class SuruculerComponent {}
